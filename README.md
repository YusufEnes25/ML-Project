# ML-Project
Built a robust tabular data classifier using XGBoost. Includes an automated preprocessing pipeline, L2 regularization, and micro-tuned hyperparameters to prevent data leakage and overfitting.
